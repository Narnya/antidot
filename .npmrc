# React Native / Expo require a flat node_modules layout; pnpm's default symlinked
# layout breaks Metro resolution. Use the hoisted node-linker for this monorepo.
# See https://docs.expo.dev/guides/monorepos/ and pnpm docs.
node-linker=hoisted
