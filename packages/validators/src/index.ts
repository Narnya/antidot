// @social-events/validators — shared validation schemas (Zod), reused by client UX
// and server-side (Edge Functions). Client validation is UX-only; authoritative
// validation of sensitive operations happens server-side.
// Placeholder for INFRA-002 — no schemas yet (Zod is added in a later task).
export {};
