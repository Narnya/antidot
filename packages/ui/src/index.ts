// @social-events/ui — design system.
// INFRA-009: base design tokens only (semantic colors, spacing, radius, typography,
// shadows, zIndex). No components yet; no React/RN dependency. Presentation tokens
// only — no product rules (access control, trust score, location) encoded here.
export * from './tokens';
