// @social-events/analytics — shared analytics taxonomy / wrapper (PostHog later).
// Strict privacy boundary: never send exact location, raw message bodies, report
// descriptions, private profile data, or the raw trust score — only ids / enums /
// buckets / counts. Event names come from /docs/10_ANALYTICS.md (do not invent).
// Placeholder for INFRA-002 — no analytics code or SDK wiring yet.
export {};
