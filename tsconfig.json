{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "//": "Root anchor for editors. Per-package tsconfig.json files do the real typechecking; run `pnpm typecheck` (recurses into packages).",
  "include": [],
  "exclude": ["node_modules", "**/dist", "**/build"]
}
