// Placeholder header. No session/user info — there is no auth in this shell.
export function AdminHeader() {
  return (
    <header className="admin-header">
      Placeholder shell — no auth, no Supabase / service role, no admin data.
    </header>
  );
}
