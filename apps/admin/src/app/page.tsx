import { PlaceholderPage } from '@/components/admin-shell/PlaceholderPage';

export default function Overview() {
  return (
    <PlaceholderPage
      title="Overview"
      subtitle="Infrastructure placeholder only. The admin dashboard shell renders here; real overview metrics will be implemented later (server-side, audited)."
    />
  );
}
