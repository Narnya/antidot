import { PlaceholderPage } from '@/components/admin-shell/PlaceholderPage';

export default function Settings() {
  return (
    <PlaceholderPage
      title="Settings"
      subtitle="Infrastructure placeholder only. No configuration; secrets and service role are server-side only."
    />
  );
}
