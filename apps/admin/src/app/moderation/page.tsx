import { PlaceholderPage } from '@/components/admin-shell/PlaceholderPage';

export default function Moderation() {
  return (
    <PlaceholderPage
      title="Moderation Queue"
      subtitle="Infrastructure placeholder only. Real moderation data and actions will be implemented later."
    />
  );
}
