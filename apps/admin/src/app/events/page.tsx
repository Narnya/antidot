import { PlaceholderPage } from '@/components/admin-shell/PlaceholderPage';

export default function Events() {
  return (
    <PlaceholderPage
      title="Events"
      subtitle="Infrastructure placeholder only. No event data; exact location access is server-side and audited when implemented."
    />
  );
}
