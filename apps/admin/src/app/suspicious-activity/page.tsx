import { PlaceholderPage } from '@/components/admin-shell/PlaceholderPage';

export default function SuspiciousActivity() {
  return (
    <PlaceholderPage
      title="Suspicious Activity"
      subtitle="Infrastructure placeholder only. System/AI flags are assistive signals, never automatic enforcement."
    />
  );
}
