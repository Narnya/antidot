import { PlaceholderPage } from '@/components/admin-shell/PlaceholderPage';

export default function Messages() {
  return (
    <PlaceholderPage
      title="Messages"
      subtitle="Infrastructure placeholder only. No message data; reported-message context is server-side only when implemented."
    />
  );
}
