import { PlaceholderPage } from '@/components/admin-shell/PlaceholderPage';

export default function Users() {
  return (
    <PlaceholderPage
      title="Users"
      subtitle="Infrastructure placeholder only. No user data, no trust internals; raw trust score is never shown."
    />
  );
}
