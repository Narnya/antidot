import { PlaceholderPage } from '@/components/admin-shell/PlaceholderPage';

export default function Reports() {
  return (
    <PlaceholderPage
      title="Reports"
      subtitle="Infrastructure placeholder only. Report review is not implemented; reports are private and server-side only."
    />
  );
}
