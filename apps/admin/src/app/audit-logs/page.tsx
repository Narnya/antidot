import { PlaceholderPage } from '@/components/admin-shell/PlaceholderPage';

export default function AuditLogs() {
  return (
    <PlaceholderPage
      title="Audit Logs"
      subtitle="Infrastructure placeholder only. Audit logs are append-only and server-side; every moderation-sensitive action must create one."
    />
  );
}
