// (app) → create. Renders the Create Activity form (ACT-005) on the mock
// repository. Reachable from home / My Circles.
import { CreateActivityScreen } from '../../src/features/activities/screens/CreateActivityScreen';

export default function Create() {
  return <CreateActivityScreen />;
}
