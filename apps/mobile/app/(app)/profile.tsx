// (app) → profile. My Profile (own — ACT/T4).
import { ProfileScreen } from '../../src/features/activities/screens/ProfileScreen';

export default function MyProfile() {
  return <ProfileScreen />;
}
