import { Placeholder } from '../../components/Placeholder';

export default function Home() {
  return (
    <Placeholder routeName="(app) → home" note="Future authenticated app tabs. Placeholder only." />
  );
}
