// (tabs) → Для тебя. Activity feed (the entry surface). Landing route (/feed).
import { FeedScreen } from '../../../src/features/activities/screens/FeedScreen';

export default function Feed() {
  return <FeedScreen />;
}
