// (tabs) → Уведомления. Activity-scoped events (reminders); never others' transitions.
import { NotificationsScreen } from '../../../src/features/activities/screens/NotificationsScreen';

export default function Notifications() {
  return <NotificationsScreen />;
}
