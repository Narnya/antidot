// (app) → settings. Account hygiene: blocked list, sign out, delete account (ACT/T6).
import { SettingsScreen } from '../../src/features/activities/screens/SettingsScreen';

export default function Settings() {
  return <SettingsScreen />;
}
