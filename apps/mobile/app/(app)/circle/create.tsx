// (app) → circle/create. Create Circle (ACT-009).
import { CreateCircleScreen } from '../../../src/features/activities/screens/CreateCircleScreen';

export default function CreateCircle() {
  return <CreateCircleScreen />;
}
