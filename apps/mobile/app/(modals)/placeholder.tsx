import { Placeholder } from '../../components/Placeholder';

export default function ModalPlaceholder() {
  return (
    <Placeholder
      routeName="(modals) → placeholder"
      note="Future apply / report / block modals. Placeholder only."
    />
  );
}
