import { Stack } from 'expo-router';

// (public) — future welcome / auth / invite flow. Placeholder navigator only.
export default function PublicLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
