import { Placeholder } from '../../components/Placeholder';

export default function Welcome() {
  return (
    <Placeholder routeName="(public) → welcome" note="Future welcome / auth / invite flow. Placeholder only." />
  );
}
